export function initNavigation(){const menuToggle=document.querySelector('.menu-toggle');const navLinks=document.querySelector('.nav-links');if(menuToggle&&navLinks){menuToggle.addEventListener('click',()=>{navLinks.classList.toggle('open');const expanded=menuToggle.getAttribute('aria-expanded')==='true'||false;menuToggle.setAttribute('aria-expanded',!expanded);});}
const currentPath=window.location.pathname.split('/').pop()||'index.html';document.querySelectorAll('.nav-links a').forEach(link=>{if(link.getAttribute('href')===currentPath){link.classList.add('active');}});
}
export function initLocalStorageGreeting(){const visitorKey='kairos_visitor_visited';const visitedBefore=localStorage.getItem(visitorKey);if(!visitedBefore){localStorage.setItem(visitorKey,'true');console.log('Welcome to Kairos Waves Technologies for the first time!');}else{console.log('Welcome back to Kairos Waves Technologies!');}}
document.addEventListener('DOMContentLoaded',()=>{initNavigation();initLocalStorageGreeting();});
